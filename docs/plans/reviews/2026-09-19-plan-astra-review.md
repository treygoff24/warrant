# Plan review round 1: Astra low, dispositions

Date: 2026-09-19. Subject: `docs/plans/2026-09-19-warrant-v1-plan.md` at its first lint-clean draft. Reviewer: Astra (`delegate codex safe --model astra --reasoning-effort low`, 469 seconds, read-only). Raw report: `2026-09-19-plan-astra-review-raw.md`. Adjudicator: Fable, checking every cited spec passage in `docs/specs/2026-09-16-warrant-v1-spec.md` before ruling.

Astra reported 2 blockers, 15 majors, 2 minors, 1 note, and would not approve the draft. Every finding is dispositioned below as adopted, adopted with a change, or rejected, with the evidence and the patch that landed.

| # | Severity | Finding | Disposition | Evidence and patch |
|---|---|---|---|---|
| 1 | blocker | S7 can fail or go stale before R14 | Adopted | Spec line 1545: "A changed build or retrieval configuration needs an applicable rerun"; line 1857: "S7 must pass before the R14 outcome comparison." W1.G now requires S7 met and sends a not-met result to Trey as a rethink. New W2.14 reruns the approved protocol unchanged against the reviewed M2 build and rebinds the lock; W2.C1 waits on it |
| 2 | blocker | M1 can close before S1 is resolved; no adoption task | Adopted | Spec 21 M1 scope: "Resolve S1". W1.G had no path to W1.5 or W1.D10. New W1.16 applies D10 either way (pinned instrument with tested capability label, or fallback with labeled contracts); W1.G depends on it; W2.1 depends on W1.G only |
| 3 | major | Signing acceptance omits human confirmation and trusted environment | Adopted | Spec line 1368: the tool "renders its effects and classification from those bytes, and asks the human to confirm"; "the signing environment never executes candidate code or plugins." W2.7 delivers, invariants, acceptance, demo 7, and the invariant table now require render-from-final-bytes, confirmation, refusal writes nothing, no candidate code or hooks in the sign process, synthetic fixture keys named as such, and no durable record under the placeholder domain (D2 stays open) |
| 4 | major | Circular behavioral dependencies among W2.3, W2.5, W2.7, W2.8, W2.9 | Adopted | Spec HC4 and 3.1 (core owns receipt and verifier, authority owns signature verification, core depends on nothing). The check receipt statement moved into W2.5 so every check emits its receipt; W2.5 now depends on W2.7 and W2.8 for approval; batch drafting from a receipt moved from W2.7 to W2.9; W2.9 depends on W2.8; Interfaces define `core::receipt::verify` taking a `RulingVerifier` trait that `authority` implements and `cli` composes |
| 5 | major | W1.2 and W1.6 need `warrant model` before W1.3 lands; W1.13 uses render without W1.12 | Adopted | W1.2 now owns `commands/model.rs` and delivers the initial model command with edges unresolved; W1.6 depends on W1.2; W1.13 depends on W1.12 |
| 6 | major | M1 lacks producers for declared facts, registry entrypoints, framework adapters, required evidence | Adopted with a change | Spec 21 M1 exit names "examples, consumers, and required evidence"; 5.4 names registry declarations and framework adapters as versioned instruments. New W1.8 writes declared facts into the model, matches registry declarations, and ships the framework adapter Atlas needs if W0.8's manifest records one (adapters for shapes Atlas does not use are carried forward, not built). W1.11 returns required evidence rows. Obligation evaluation stays in M2 |
| 7 | major | Corpus reduced to Atlas without a ruling | Adopted | Spec 17.3 names the five open-source shapes; S1 and S6 measure over the corpus. New ruling P12: W0.8 pins all six members with dependency trees; an unobtainable member is an unresolved prerequisite, never a smaller denominator |
| 8 | major | No corpus stage; budgets recorded, not gated | Adopted | Spec 18.1 lists corpus smoke in the gate; 17.6: "A regression past an applicable budget fails the corpus job." New stage `45-corpus.sh` (W0.6) with semantic expectations; W2.11 asserts the cold and memory budgets; W2.9 asserts the verify budget; ruling P13 covers private members in CI |
| 9 | major | Stage discovery lets checks vanish; P5 accepts unexecuted CI; no MSRV matrix | Adopted | Spec 17.1: "Every fixture runs in CI on every change"; 18.2: "CI builds on MSRV and stable." Gate asserts `scripts/stages/REQUIRED`; acceptance script asserts item inventory; W0.1 adds stable and MSRV jobs; P5 amended: CI must be observed executing before W0.G, which needs one gated GitHub push from Trey, or W0.G reports the prerequisite blocked |
| 10 | major | P6 pins every section 19 dependency | Adopted | Spec line 1920: "They are candidate pins"; "Optional entries are not mandatory dependencies." W0.2 rechecks and records in `docs/research/2026-09-dependency-recheck.md`, adds only what the slice needs; P6 amended with the rework cost |
| 11 | major | Report ingestion lacks predicate evaluation | Adopted | Spec 9.3 defines `absent: true` and `at_least` requirements over canonical rows. W2.4 evaluates them with forbidden-finding and threshold cases; new invariant and table row |
| 12 | major | Comparison can freeze an unreviewed reference policy | Adopted | Spec line 1815 freezes the "human-reviewed reference policy" before agents run. W2.11 creates `docs/acceptance/2026-M2-frozen-inputs.md`; W2.C1 waits on W2.14 and approves the full reference policy digest; a changed input voids the approval |
| 13 | major | No critical review of the M2 diff | Adopted | Spec 18.2: critical for "every milestone diff." New W2.R (Fable, judgment) after W2.11 and before requalification and comparison |
| 14 | major | Opus retry executor breaks reviewer decorrelation | Adopted with a change | Astra proposed dynamic pools; the grammar's routing is static. Retry executor is now Sol `xhigh` (same OpenAI family as Astra), so no executor route can share a family with a reviewer or adjudicator seat. P2 amended |
| 15 | major | `retrieval` and `fault` missing from the core tree; schema files unowned | Adopted | Both modules added to the pre-declared tree; W0.2 creates one schema stub per document, later tasks replace their stub and own their `schemas/<name>.json`; the acceptance script requires zero stubs among this plan's documents |
| 16 | major | Demo 9 expects `verified` from an unsigned receipt | Adopted | Spec 10.5 step 9: an absent producer signature "makes the result `advisory`"; steps 4 and 5 leave bindings unverified without `--recompute`. Demo 9 and W2.9 acceptance now expect advisory, verified-with-unchecked, and verified on the matching inputs |
| 17 | major | M0 promises section 5.7 proofs it cannot run; no `--verify-generated` | Adopted | Spec 5.7 lists five fixtures. W0.6 keeps the M0-available proofs, assigns registry removal to W2.2 and unreadable-file to W2.1 by name; W0.5 delivers `--verify-generated` with generated-drift and generated-absent |
| 18 | minor | Four invariant proofs test weaker properties | Adopted | W0.3 test makes the worktree copy unreadable so an index read cannot succeed by accident; W0.7 parses the whole output stream; W1.C2 has Trey confirm the protocol carries no private text; W2.5 adds a facet-input mutation matrix |
| 19 | minor | `policy effective`, `evidence list`, focused-check selectors, changed-source diagnostics unowned | Adopted with a change | `policy effective` to W1.9, `evidence list` to W2.4, `check --contract --instance` to W2.5; changed-source diagnostics after editing events carried forward by name (spec 21 puts additional diagnostic adapters in M5) |
| 20 | note | Ruling assessment | Adopted | P2, P5, P6 amended as above; P7, P8, P9, P10, P11 wording tightened per the note; P1, P3, P4 retained. P12 and P13 added |

Nothing was rejected outright. Two findings were adopted with a different mechanism than Astra proposed (14: static family choice instead of dynamic pools; 6: framework adapters limited to what Atlas needs). Those two are the points a re-review should test.

After the patch: `plan-lint` clean with no advisories, `--waves --check` and `--check-routes` pass, 46 tasks in 29 waves. Round 2 is a scoped Astra re-check of the revised dependencies and acceptance conditions.

## Round 2: scoped re-check of the revised plan

Date: 2026-09-19. Same route and effort, 371 seconds, over the round-1-revised plan with the disposition table above as input. Raw report: `2026-09-19-plan-astra-review-2-raw.md`. Astra's status line: "Round 1 status: 11 closed, 7 incomplete, 1 regressed, 1 disputed. New findings: 0 blocker, 1 major, 1 minor, 0 note." Astra did not approve as revised and named R1-1, R1-4, R1-6, R1-8, R1-9, R1-12, R1-13, R1-15, and N1 as blocking.

| # | Severity | Finding | Disposition | Evidence and patch |
|---|---|---|---|---|
| R1-1 | blocker | S7 can pass before W1.16 changes the executable; W1.G accepts a stale lock | Adopted | Spec 12.3 and 17.7 bind qualification to the build. W1.16 now blocked by W1.4 as well as W1.D10; W1.15 blocked by W1.16; W1.15 binds build, configuration, sample, criteria, and policy in the lock; W1.G checks the lock's build digest equals the reviewed HEAD's and sends S7 back to W1.15 otherwise |
| R1-4 | major | Verifier boundary still assigns recomputation to `core`, which cannot reach the tree | Adopted | Spec 3.1 (core depends on nothing), 10.5 steps 2 to 5 need snapshot, inventory, and model. Interfaces now define an injected `Recompute` trait implemented in `warrant-cli` and `RulingVerifier` in `warrant-authority`, with the step-to-trait map; W2.9 owns the composition and a `--recompute` conformance case over the real composition that fails on an altered inventory |
| R1-6 | major | Atlas-only adapters violate spec 5.4 given P12's Next.js member; loaders and config-referenced entrypoints unassigned | Adopted, round-1 "adopted with a change" withdrawn | Spec 5.4 requires adapters for frameworks the corpus uses; P12 pins a Next.js app. W1.8 now selects adapters from the full manifest (at least Next.js file-system routes), each versioned and enabled per manifest, adds declared loaders with `config_reference` edges and config-referenced entrypoints, and gains an invariant plus verify row that core reaches no framework symbol. The carried-forward line now defers only frameworks no member uses |
| R1-8 | major | P13 lets local runs stand in for the CI budget evidence spec 17.6 requires | Adopted as a named blocked prerequisite | Spec 17.6 measures the Atlas budgets in CI; D4 restricts publication, not private execution. No authorized private CI path exists today. P13 now records `CI corpus job with Atlas: blocked prerequisite` at every milestone close, forbids calling a local run CI, and puts the runner decision (Forgejo Actions on the estate or a self-hosted runner with the corpus) in front of Trey with the plan approval |
| R1-9 | major | Unconditional ten-item inventory in `acceptance.sh` breaks W0.1's bootstrap | Adopted (regression fixed) | The script now reads `tests/acceptance.d/REQUIRED`, appended by each task that adds an item; `--final` requires 01 to 03 at M0, 01 to 05 at M1, 01 to 10 at M2, plus the Atlas-ran and zero-stub checks scoped to the milestone. W0.1 creates an empty REQUIRED; W0.6, W1.15, W2.5, W2.9, W2.10, W2.13 append their items and own the file |
| R1-12 | major | Frozen-inputs record can be obsolete when W2.C1 approves it | Adopted | Spec 17.5 freezes the inputs the agents will run against. W2.11 now drafts the record; W2.14 finalizes it after review fixes and requalification and owns it; W2.C1's acceptance names the identities Trey approves and requires them to equal the lock and what W2.13 uses |
| R1-13 | major | W2.R misses the artifacts written after it | Adopted | Spec 18.2: critical review for every milestone diff. New checkpoint W2.R2 (Fable, judgment) after W2.13 reviews the delta since W2.R's HEAD; W2.G is blocked by it |
| R1-15 | major | Producers do not own their schema stubs or generated JSON; module registration unassigned; profile schema missing | Adopted | HC10. Every producing task now owns `crates/core/src/schema/<name>.rs` and `schemas/<name>.json` and says it replaces the stub; `warrant.profile` added to the catalog and owned by W2.6; module pre-declaration extended to model (W1.1), lang-ts (W1.2), snapshot (W0.3), authority (W2.7); W1.16 owns its fallback fixtures under `tests/conformance/policy/labeled`; W2.10 owns `scripts/stages/REQUIRED` for the mutants stage |
| R1-18 | minor | Unreadable-worktree test still permits a tolerated read attempt | Adopted | W0.3 routes every worktree read through one function with a test-build attempt counter; the invariant and verify row assert zero attempts for index and commit snapshots, with a worktree snapshot proving the counter binds |
| N1 | major | W2.14 requalifies against the M1 policy digest while the comparison uses W2.11's full policy | Adopted | Spec 12.3 binds qualification to the policy. W2.14 now reruns against the full Atlas reference policy W2.11 drafted, recorded as a new policy identity, with the same sample, criteria, and budget; the lock, the frozen-inputs record, and the comparison inputs must name identical identities, and a mismatched-policy fixture is refused credit |
| N2 | minor | Lock section says P1 to P11; experiment summary names Opus | Adopted | Lock section now P1 to P13 and carries the P13 runner question; experiment summary names Sol `xhigh` per the routing block |

Nothing rejected. The round-1 disposition of #6 is superseded by R1-6 above. After the patch: `plan-lint` clean with no advisories, `--waves --check` and `--check-routes` pass, 47 tasks in 30 waves. Round 3 is a scoped Astra re-check of these eleven items only.

## Round 3: scoped re-check of the round-2 patch

Date: 2026-09-19. Same route and effort, scoped to the eleven round-2 items. Raw report: `2026-09-19-plan-astra-review-3-raw.md`. Astra closed R1-1, R1-6, R1-8 (as a planning disposition: "Atlas CI remains unproved and must not be reported as satisfied"), R1-12, R1-13, R1-18, N1, and N2, and named three blocking items, two of them regressions the round-2 patch introduced.

| # | Severity | Finding | Disposition | Evidence and patch |
|---|---|---|---|---|
| R1-4 | major | Step 6 compared instrument identities against the receipt's own bytes, not the lock at the subject tree; step 7 had no way to load the actual artifacts | Adopted | Spec 10.5 step 6: "match the lock at that tree"; step 7 requires artifacts "present". `Recompute` now also returns the lock bytes at the tree; a third injected operation `Artifacts` loads receipts, reports, and rulings from the local store by digest; the step map says step 6 never uses the receipt's own copy. W2.9 composes both and adds altered-lock, missing-artifact, and mismatched-subject cases through the real CLI composition |
| R1-9 | major (regression) | `--final` required item 01 at M0 and M1, but only W2.10 creates it, so M0 could never close | Adopted | The final inventories are now 02 to 03 at M0, 02 to 05 at M1, 01 to 10 at M2, with the note that W2.10 creates item 01 |
| R1-15 | major (part regression) | Pre-declaring a stub module does not assign the call site; W1.8 and W1.16 owned no composition path. W2.14 promised a mismatch fixture it owned nowhere | Adopted | The pre-declaration device now says the declaring task wires each stub into the real composition path with an empty result (model build calls `frameworks` and `references`; `query` not-implemented; `native` unsupported; `batch` empty), so the filling task replaces a module body only; W1.1, W1.2, W0.3, and W2.7 say so in their delivers. W2.14 owns a new `tests/conformance/qualification` area, run by the W0.6 runner, holding the matching and mismatched-policy cases; the area list and invariant table name it |

After the patch: `plan-lint` clean with no advisories, `--waves --check` and `--check-routes` pass, 47 tasks in 30 waves, wave map unchanged. No fourth round was run: the three fixes are local to prose Astra had already bounded, and the coordinator judged a further scoped check to cost more than it would catch. Trey can ask for one with the approval.
