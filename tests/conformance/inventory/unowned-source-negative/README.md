# Unowned source: negative boundary

Hard control: `inventory.owned` applies to first-party source, not arbitrary
unknown files. This variant keeps an unclaimed Perl file visible as `unknown`
without laundering it into the source denominator.
