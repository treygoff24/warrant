# Acceptance items

Each executable file in this directory is one numbered demonstration. It must
print either `ACCEPT <n>` when the behavior is demonstrated or
`REFUSE <n> <reason>` when it cannot be demonstrated.

`REQUIRED` lists the item numbers that must exist and be executable. The list is
empty until a task adds its first acceptance item.
