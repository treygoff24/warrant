# Generated ignored: negative boundary

A glob declaration matches one tracked file and one gitignored file. The
tracked match is in the snapshot, so the declaration is present: the tracked
file is `generated`, the ignored copy stays `ignored`, and nothing is
recorded as `generated-absent`.
