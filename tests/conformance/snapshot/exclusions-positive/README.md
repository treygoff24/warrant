# Snapshot exclusions: positive

Hard control: a clean worktree has no deliberately excluded, unread, or
submodule paths. The negative and break-control siblings exercise the boundary
and every section 4 exclusion available at M0.
