# M0 milestone review and gate record

Date: 2026-09-24. Subject: the M0 diff of Warrant, base `9040c7c` (the pre-build commit) to spine head `c0aaea6` (79 commits, 253 files, 15,916 insertions), branch `plan/warrant-v1-m0-m2`. Gate: W0.G, "Close M0 after the milestone review", an oracle gate held by the coordinator seat (Fable per plan P15; this session holds the seat under Trey's 2026-09-23 word). Reviewers, three families, all `safe` mode on an isolated copy of the tree at `c0aaea6` with the full diff, the gate log, the spec, and the plan materialised under `review-input/` (deleted after the round):

| Seat | Route | Handle |
|---|---|---|
| 1 | `delegate codex safe --model astra --reasoning-effort xhigh` (GPT-6 Astra) | codex-62 |
| 2 | `delegate claude safe --model opus --reasoning-effort xhigh` (Claude Opus 5.5) | claude-16 |
| 3 | `delegate omp safe --model opencode-go/glm-5.3` (GLM 5.3) | omp-32 |

Every seat received the same brief (`review-input/M0-review-brief.md`; seat 3's copy adds an output-discipline clause) carrying all three plan personas (refuter, conventions, test-skeptic), the spec 21 M0 exit row, the named coordinator-merged commits that no panel had reviewed, the open beads dispositioned to this gate, and the carried hazards. Decorrelation is by model family, not by persona.

## Verdict

M0 exits at `f6f9583` on `plan/warrant-v1-m0-m2`. Three reviewers from three families (Astra on Codex, Opus 5.5 on Claude, GLM 5.3 on OMP) reviewed the whole M0 diff at `c0aaea6`; every finding has a disposition below (P1–P65); the adopted findings were fixed across three bounded rounds (rulings F42, F44, F47) with a fresh two-family review after round 1 and an Astra re-check after round 2. The exit statement below is bound at `949130f` and holds at `f6f9583` (round 3 changed verification hashing for file-type drift, the Cargo copy's toolchain files, the `--commit` resolution path, and the corpus runner's unit tests; no clause of the statement moved). At `f6f9583`: `scripts/gate.sh` printed `gate: OK` (32 test binaries, 183 passed, 0 failed; the corpus stage now prints `corpus: runner unit tests 1` before the six members; `budget: snapshot 2621/3000`), `tests/acceptance.sh --final M0` with the private Atlas member present exited 0 (`acceptance: OK`, `corpus: atlas ran`, `final: 6 in-scope document(s) checked at M0`), and CI run `35966837232` executed the gate green on `gate (stable)` and `gate (MSRV 1.96.0)`. The spec-21 M0 row is checked clause by clause in the exit statement's table.

## Gate and CI evidence

- Head under review, `c0aaea6`: `scripts/gate.sh` in the coordinator's isolated target directory printed `gate: OK` with every REQUIRED stage executed (fmt, clippy, deny, test, deps, schema, corpus, conformance, budget) on rustc 1.98.1; `tests/acceptance.sh` in plain mode printed `acceptance: OK` (items 02 and 03 ACCEPT). The corpus stage of that run skipped the private member (`corpus: atlas not run: private member unavailable`), which is what the panel's P9 reported.
- Final mode at `c0aaea6`, run by the coordinator after the panel: the private Atlas artifacts survived on this machine from the W0.8 lane directories; a full corpus directory (`WARRANT_CORPUS_DIR=/var/tmp/warrant-corpus-full`) verifies 6/6 members against `tests/corpus/manifest.yaml` (`scripts/corpus.sh verify`: `corpus: verified 6/6 members`). `tests/acceptance.sh --final M0` then exited 0: `mode: final M0`, `gate: pass`, `ACCEPT 2`, `ACCEPT 3`, `final: 6 in-scope document(s) checked at M0`, `acceptance: OK`; its gate log carries `corpus: atlas ran` and the five public members. The Atlas expectation (`files: 55358`) matched the product at that head. The private artifacts stay outside the repository and outside every lane's copy except by that environment variable; nothing from inside them is quoted here.
- Final mode at the fix-merge head `13e45d0` (lanes A, B and C fully merged, A11 and B23 included): `scripts/gate.sh` printed `gate: OK` with `corpus: atlas ran` and the five public members; `tests/acceptance.sh --final M0` exited 0 with `mode: final M0`, `gate: pass`, `ACCEPT 2`, `ACCEPT 3`, `final: 6 in-scope document(s) checked at M0`, `acceptance: OK`. The Atlas expectation matched the product after the fix pass as well.
- CI at `c0aaea6`: Trey authorised one GitHub push on 2026-09-24 (`ask`, answered "Yes"); `plan/warrant-v1-m0-m2` was pushed to `github` and run `35953669425` executed `gate (stable)` and `gate (MSRV 1.96.0)`. Both jobs ran fmt, clippy, deny, test, deps and schema green and went red at the corpus stage with `corpus.sh fetch exited 1` for every public member and no further text, because `tests/conformance/corpus/run.py` dropped the child's stderr (fixed by lane C item C1).
- CI at `8f78684` (the A-plus-C merge head, pushed under Trey's overnight pre-approval of GitHub pushes, recorded on `warrant-29b`): run `35957443610`, both jobs red at the corpus stage again, now with the cause visible: every public member fails `fetch: <name> verify` with `corpus: <name> dependency digest mismatch` after clone, install and archive succeed, and the source digests match. The runner has node v22.23.2 against the manifest's `node_observed` v26.9.0. Local reproduction the same night: with node v26.9.0 and npm 12.0.2, `scripts/corpus.sh fetch commonjs-library` and `fetch pnpm-project-references` both re-verify against their recorded digests, so the dependency archive is reproducible and the runner's Node is the difference (npm and pnpm write different `node_modules` metadata under a different Node). Fix C4 (lane C): `actions/setup-node@v7` pinned to the manifest's Node in both gate jobs, and the deps stage checks `ci.yml` and `tests/corpus/manifest.yaml` agree, red-proofed; merged at `31c3cbd` and pushed. Run `35958360229` at that head, with Node 26.9.0 installed, failed every public member the same way, so Node was not the difference. The cause, found the same night: the dependency archives record file modes, npm and pnpm create `node_modules` under the process umask, and the digests were recorded under umask 002 (a local archive holds 9,948 files `-rw-rw-r--`); GitHub runners use 022. The same member re-fetched locally under `umask 022` mismatches and under 002 verifies. Fix C5 (lane C): `umask 002` pinned inside `corpus.sh`'s `fetch_one`, so the caller's environment no longer decides the bytes; no digest changes. The durable alternative, normalising modes with `tar --mode` and regenerating all six digests by repacking the existing artifacts, including the private member's, is recorded on `warrant-61z` as a separate decision. The C5 head `23db295`, pushed the same night, ran green on both jobs (`gate (stable)`, `gate (MSRV 1.96.0)`) with the corpus stage executing the five public members on the runner: the W0.G clause "CI observed executing the gate on stable and MSRV" is met in full. CI at the fix-merge head (lane B included): run `35960855315` at `13e45d0`, `gate (stable)` and `gate (MSRV 1.96.0)` both green, the corpus stage executing the five public members on the runner (the private member stays outside CI by design).
- CI at the round-2 A-plus-C head `d1f94f6` (push 8): run `35963405310`, both jobs red at the test stage on exactly one test, A13's `declared_unborn_submodule_matches_git_add`, because the runner's Git 2.55.0 accepts `git add -A` over a declared submodule with an unborn checkout while the cell's Git 2.47.3 refuses it and the test assumed the refusal. Every other stage and test was green on both jobs. Ruling F46 (on `warrant-29b`): Warrant refuses `unborn-submodule` on every Git version and the test observes its oracle instead of assuming it (A15). Final mode at the round-2 head `949130f` (A12–A15, B24–B34, C6–C7 and the budget raise merged): `scripts/gate.sh` printed `gate: OK` with `corpus: atlas ran` and the five public members (budgets cli 1231/4000, inventory 1837/2500, snapshot 2621/3000); `tests/acceptance.sh --final M0` exited 0 with `mode: final M0`, `gate: pass`, `ACCEPT 2`, `ACCEPT 3`, `final: 6 in-scope document(s) checked at M0`, `acceptance: OK`. CI at the round-2 head `949130f` (push 10): run `35965479986`, `gate (stable)` and `gate (MSRV 1.96.0)` both green, the corpus stage executing the five public members; the intermediate `0e56a4e` (push 9, run `35964466689`) was green as well and exercised the Git 2.55.0 branch of the A15 oracle.

## M0 exit statement

Each clause of the spec 21 M0 row, the test that binds it at the round-2 head `949130f` (the fix-merge head `13e45d0` plus the round-2 fixes), and the three seats' verdicts at `c0aaea6` before the fix pass. Where a seat said a clause did not hold, the column names the panel finding and the fix item that closed it; the fresh two-family review of the fix diff (below) is the evidence that the fix holds.

| Clause | Bound by (at `949130f`) | Panel at `c0aaea6` | After the fix pass |
|---|---|---|---|
| Staged and committed reads ignore unrelated worktree bytes | `crates/snapshot/src/tests.rs::object_snapshots_read_exact_blobs_without_touching_dirty_worktree` (read-attempt counter zero while the worktree is dirty; the worktree drive above zero proves the counter binds); `crates/cli/tests/conformance.rs::snapshot_index_acceptance` over `tests/conformance/snapshot/index-break-control`; `tests/acceptance.d/02-snapshot-index.sh` | GLM: holds. Opus: holds for `Snapshot::read`, inventory discovery bypasses it (P2). Astra: does not hold end to end, the live manifest read bypasses the snapshot (P3) and discovery reads disk (P2) | Holds. P2 fixed by B1 and B17 (discovery reads only through the snapshot's reader; `Unread` excluded; tests over a real capture), P3 by B2 (object snapshots read `warrant/warrant.yaml` from the object they capture) |
| Tracked ignored files remain present | `crates/snapshot/src/tests.rs::worktree_capture_preserves_real_index_and_tracked_ignored_files`; `tests/conformance/snapshot/exclusions-negative` (force-added `tracked.log`) through `snapshot_conformance`, presence pins from W0.6's fix round | All three: holds | Holds, unchanged |
| Missing or unread input is explicit | `crates/snapshot/src/tests.rs::input_errors_are_structured_exit_two_and_never_call_consumer`; `exclusions-break-control` `unread` set (`oversize`, `external-symlink`); `crates/inventory/tests/inventory.rs::unread_snapshot_entry_preserves_read_failure_in_completeness`; every failure exits 2 with a `warrant.error/1` document | GLM: holds. Opus: holds. Astra: does not hold throughout inventory, an unread configuration file still shaped discovery (P2) and an ignored generated output present on disk lost its `generated-absent` record (P6) | Holds. P2 as above; P6 fixed by B4 (presence decided against captured source paths; literal and glob fixtures `generated-ignored-{positive,negative,break-control}`) |
| Colocated tests retain their class | `crates/inventory/tests/inventory.rs::colocated_test_keeps_its_class_and_owner`; `tests/conformance/inventory/colocated-test-{positive,negative,break-control}`; `tests/conformance/demo.py` item 3 through the CLI (`tests/acceptance.d/03-inventory-unowned.sh`) | GLM: holds (module ownership through the CLI deferred to W2.1, F39). Opus: holds on a synthetic snapshot. Astra: holds in the tested root-directory case, not across CLI invocation locations (P4) | Holds. P4 fixed by B3 (root resolved once with `git rev-parse --show-toplevel`; root-versus-subdirectory equivalence tests) |
| S3 resolves native capture or keeps the Git path | Git path kept: `crates/snapshot/src/native.rs` returns `None`, the feature is off by default; `crates/snapshot/tests/native.rs` six parity cases in the gate; `docs/spikes/2026-09-S3-native-capture.md` `Status: met` with the corpus-wide criterion recorded as unmet | All three: holds (Astra: the six tests qualify only their six cases) | Holds; `warrant-ail` carries the corpus criterion to W1.G |
| This proves source identity and inventory, not architectural acceptance | The rows above; `crates/cli/tests/cli.rs::every_stub_returns_the_error_contract` for the stubs | Astra: identity does not hold (P1 worktree read returned filesystem bytes, P2, P5 gitlinks carried at the indexed commit, P7). Opus: worktree byte identity does not hold (P1) nor the exec-bit rule (P10). GLM: holds | Holds. P1 fixed by A2, P5 by A3, P7 by A5, P10 by A6, each with a Git-oracle test |

The coordinator's statement: M0 exits. Source identity and inventory are proved at `949130f` by the tests named above, every one of which ran in the gate that `tests/acceptance.sh --final M0` executed with the private Atlas member present. The panel's verdict at `c0aaea6` was split (Astra: do not admit; Opus and GLM: admit after the majors); the coordinator adopted every finding, fixed the blockers and majors before admitting rather than accepting them, put the fix diff through a fresh two-family review, fixed what that review found in a second bounded round (F44), and had the round-2 diff re-checked by one seat (below), so the split is resolved by evidence rather than by vote. What M0 does not prove is unchanged: architectural acceptance and the corpus-wide native-capture criterion. CI is green through the corpus stage since the umask fix (below).

## Panel findings and dispositions

Three seats returned full reports (Astra: 2 blockers, 7 majors, 2 minors; Opus: 9 majors, 15 minors, 4 notes; GLM: 4 majors, 8 minors, 3 notes). Verdicts: Astra "do not admit M1"; Opus and GLM "admit M1 after the majors below". The coordinator read every cited line at `c0aaea6` before dispositioning; a finding is adopted on that reading, not on the vote. Findings are grouped by subject; the seat column names who reported it (A = Astra, O = Opus, G = GLM, with the seat's own number). Disposition vocabulary: **fixed** (by the F42 fix pass, lane and item named; the fix diff gets its own fresh review before merge), **accepted with reason** (bead or plan row named), **recorded** (no action beyond this record), **refuted** (with evidence). Severity is the coordinator's after verification.

| # | Subject | Seats | Severity | Disposition |
|---|---|---|---|---|
| P1 | Worktree `Snapshot::read` returns filesystem bytes where index and commit reads return blob bytes for the same identity (`crates/snapshot/src/lib.rs:94`; `tests.rs::clean_filter_files_hash_like_git_add` pins the CRLF bytes). Spec 4.2 violated. | A1, O3 | blocker | Fixed: lane A item A2 (return the canonical blob bytes after verification; test compares all three kinds with `git cat-file`; clean-filter case added). |
| P2 | Inventory discovery reads configuration from disk (`fs::read`) and does not exclude `Unread` entries, so an external-symlink or oversize `package.json`/`tsconfig.json` still shapes units and entrypoints (`crates/inventory/src/lib.rs:314`, `:1001`, `:1042`, `:1068`, `:1221`, `:1354`). | A2, O1 | blocker | Fixed: lane B item B1 (reader callback from the snapshot; `Unread` excluded; external-symlink, oversize and captured-versus-live tests over a real capture, B17). |
| P3 | Object snapshots load `warrant/warrant.yaml` from the live worktree (`crates/cli/src/commands/snapshot.rs:22`, `manifest.rs:7`). | A3 | major | Fixed: lane B item B2 (manifest read from the selected source; worktree unchanged). |
| P4 | CLI root is `env::current_dir()`; from a subdirectory the default manifest loads silently and paths mis-join (`inventory.rs:20`, `snapshot.rs:22`). | A4, O2 | major | Fixed: lane B item B3 (`git rev-parse --show-toplevel` once; root-versus-subdirectory equivalence tests). |
| P5 | Worktree capture carries every gitlink at its indexed commit and keeps a removed submodule (`worktree.rs:120`, `:139`); Git would stage the checkout's HEAD or the deletion. | A5, O16 | major | Fixed: lane A item A3 (Git-oracle tests against `git add -A` in a temporary index). |
| P6 | An ignored generated file that is present on disk yields neither a producer nor `generated-absent` (`lib.rs:189`, `:219`, `:1374`); spec 5.3 sentence quoted in the brief. | A6, O6 | major | Fixed: lane B item B4 (presence decided against captured source paths; literal and glob fixtures). |
| P7 | The inventory document carries no snapshot manifest (spec 4.5 "Every artifact downstream carries this record") and `ignored_files` is always `Some` (`nouns/mod.rs:220`, `lib.rs:1434`); `summary.files` and `by_class` count ignored entries against spec 5.5's example. | A7, O20, G11 | major | Fixed: lane B items B5 and B14 (manifest embedded; `ignored_files` null for object kinds; `files` counts present non-ignored entries; public corpus expectations regenerated from the product; the Atlas expectation is carried, see P25). |
| P8 | W0.6's proof rows: the in-suite disabled-control tests mutate the observation document, not the product, and the key-reordering runner test named by the proof table does not exist (`conformance.rs:145`–`:232`, `:722`). | A8, O23, G8 | major | Fixed in part: lane B items B7 and B16 (reorder test with a byte-inequality check; README states the subset semantics). The product-side disabled-control proof is the W0.6-C1 receipt recorded in live-state's F40 block (scratch build at `0a6d72a` with the unowned-source control disabled, acknowledged against `25dfe37`); this record cites it and the proof table's wording now points there. |
| P9 | The M0 evidence: local acceptance ran in `mode: plain` because the private Atlas member was unavailable (`corpus: atlas not run`), and `tests/acceptance.sh --final M0` therefore fails at HEAD; the gate log carried no CI evidence. | A9, O9, G7 | major | See "Gate and CI evidence" and P25. CI evidence is now recorded there. The private Atlas artifacts survived on this machine from the W0.8 lanes; the coordinator assembled a full corpus directory (`WARRANT_CORPUS_DIR=/var/tmp/warrant-corpus-full`, `scripts/corpus.sh verify`: 6/6 members verified against the manifest digests) and ran the gate with Atlas and `tests/acceptance.sh --final M0` at the head under review and again at the fix-merge head; results in "Gate and CI evidence". Trey's bead `warrant-udz` (`COORD-W08-1`, the private CI runner) is unchanged. |
| P10 | Path-bearing summary lists (`unread`, `unowned_source`, `unknown`, `unit_aliases`, `generated_absent`) are unbounded under `--limit` (`inventory.rs:59`). | A10 | minor | Accepted with reason: bounded rendering is the NDJSON/streaming decision (P24); a bead carries it to the rendering task. The cached artifact is complete either way. |
| P11 | Bare `warrant snapshot` is a usage error; spec 4.1 makes worktree the default (`snapshot.rs:9`). | A11, O15, G1 | minor (contract) | Fixed: lane B item B6. |
| P12 | Commit kind resolves `^{commit}` and `^{tree}` from the symbolic revision in two calls; a ref update between them pairs C1 with T2 (`lib.rs:183`–`:208`). | O4 | major | Fixed: lane A item A5. |
| P13 | The built-in build-output default (`target`, `dist`, `build`, `node_modules`, `.next` segments) runs before the source-extension check, so tracked `src/build/index.ts` leaves the ownership obligation silently (`lib.rs:813`); spec 5.2 defines no such built-in. | O5 | major | Fixed: lane B item B8 (ruled: the built-in applies to untracked entries only). |
| P14 | `check_nested_repositories` walks the whole disk tree with ignores off, so the gitignored `/.worktrees/<name>/.git` makes `warrant inventory` refuse Warrant's own checkout (`lib.rs:549`); `verify_generated`'s copy walks the same tree. | O7, G12 | major | Fixed: lane B items B9 and B19 (check bounded to ancestors of non-ignored snapshot entries; the copy takes non-ignored entries only). |
| P15 | Tracked case-colliding fixture paths (`case-collision-break-control/repo/src/{Case,case}.ts`, `case-collision-directory-break-control/repo/{Src,src}`) make `warrant snapshot` on Warrant's own repository exit 2 and lose a file on a case-insensitive clone. | O8 | major | Fixed: lane B item B10 (fixtures created at test time; `git ls-files | tr A-Z a-z | sort | uniq -d` prints nothing). |
| P16 | Case collision exits 2 where spec 4.3 line 417 makes it an `analysis: incomplete` reason (`lib.rs:300`). | O14 | minor | Accepted with reason: the verdict facet that carries `incomplete` does not exist before M2; exit 2 is the fail-closed narrowing until then. Spec clarification candidate for Trey (item 19 below); bead. |
| P17 | Executable bit tested with `& 0o111`; Git's `ce_permissions` tests `& 0o100`, so 0o654 records `100755` here and `100644` in Git (`worktree.rs:288`). | O10 | minor (fidelity) | Fixed: lane A item A6. |
| P18 | An untracked nested repository is silently dropped from worktree snapshots (`worktree.rs:152`); `git add -A` records a gitlink. | O11 | minor (fail-open) | Fixed: lane A item A7 (recorded explicitly). |
| P19 | A terminal Ctrl-C that reaches the process group surfaces as a Git failure with exit 2 because cancellation is consulted only for `code == "cancelled"` (`snapshot.rs:72`, `inventory.rs:83`). | O12 | minor | Fixed: lane B item B11 (`cancel::check()` first; `killpg` test). Bead `warrant-dpp` (cooperative signal timing) is unchanged by this. |
| P20 | Snapshot cache key omits the capture inputs for object kinds, so two `max_file_bytes` values collide at one artifact path (`snapshot.rs:36`, `lib.rs:257`). | O13 | minor | Fixed: lane B item B12 (with B2 the manifest digest exists for every kind). |
| P21 | A symlinked ancestor is reported as `snapshot-unstable` after exhausting the retake (`worktree.rs:270`). | O17 | minor | Fixed: lane A item A8 (explicit `unsupported-path`). |
| P22 | The index kind runs `git write-tree` against the real index, taking `index.lock` and possibly rewriting the cache-tree extension (`lib.rs:216`); spec 4.1: "The repository's real index is never touched". | O18 | major | Fixed: lane A item A9 (temporary index copy; test holds `index.lock`). |
| P23 | Every inventory error carries the code `inventory` (`inventory.rs:40`). | O19 | minor | Fixed: lane B item B13. |
| P24 | NDJSON output promised by spec 12.2 and W0.7's delivers is absent; the inventory prints one JSON document paged in memory (`output.rs`, `inventory.rs:66`). | G3, carried item 8 | major (contract) | Accepted with reason, ruled F43: paged JSON with `truncated`/`next_cursor` is the v1 list convention M1 commands inherit; NDJSON streaming is M5 rendering by spec 21 and W0.7's delivers text over-promised. Plan delivers corrected; bead for the rendering task. |
| P25 | The Atlas corpus expectation (`files: 55358`, `ignored: 53604`) was pinned from the W0.8 lane's local run and cannot be regenerated here after P7 changes the `files` semantics. | O20, G7, G11 | major (evidence) | Fixed: lane B item B14 regenerates the Atlas expectation from the product against the verified private member (see P9); the coordinator's gate run with Atlas at the fix-merge head is the check. |
| P26 | `implicit-root-unit` overwrites `by`, so the entry no longer names its classifying rule (`lib.rs:338`; pinned by `inventory.rs:444`). | O21 | minor | Fixed: lane B item B15. |
| P27 | The inventory cursor is a plain offset unbound to the document; a page walk across a worktree change skips or duplicates entries (`inventory.rs:60`). | O22 | minor | Accepted with reason: the document carries its digest and the page is rebuilt per call; binding the cursor to the digest is part of the bounded-output decision (P24) and goes on the same bead. |
| P28 | The crate's test Git helper does not neutralise the ambient Git environment (`crates/snapshot/src/tests.rs:6`). | O24 | minor (test) | Fixed: lane A item A10. |
| P29 | The index-break-control oracle is computed after the product runs (`conformance.rs:421`). | O25 | note | Fixed: lane B item B16. |
| P30 | Inventory crate tests feed a synthetic snapshot with no `Ignored` or `Unread` entries (`inventory.rs:38`). | O26 | note | Fixed: lane B item B17 (new and W0.5 tests drive a real capture). |
| P31 | The corpus never checks worktree-kind tree parity (`run.py:83`). | O27 | note | Fixed: lane C item C3 (worktree tree against `git add -A` in a temporary index per member). |
| P32 | The cache falls back to a relative `.cache/` inside the repository when `HOME` and `XDG_CACHE_HOME` are unset (`cache.rs:13`). | O28 | minor | Fixed: lane B item B18 (explicit `cache-location` error). |
| P33 | `warrant inventory --verify-generated` (spec 5.3, W0.5 delivers) exists only as a crate function; the generated conformance cases reach it through a test driver (`lib.rs:334`, `inventory.rs:10`). | G2, carried item 6 | major (contract) | Fixed: lane B item B19 (flag wired, `generated_drift` in the summary, cases moved to the `cli` driver). Owner question closed. |
| P34 | MSRV: eleven crates at 1.90, `lang-ts` at 1.96, CI at 1.96.0, nothing checks agreement; spec 18.2 wants the MSRV stated in the manifests. | G4, carried item 4 | major (contract) | Fixed: lane C item C2 (workspace `rust-version = "1.96"`, inherited; deps stage checks agreement with `ci.yml`). Spec line 1926's 1.90 is a clarification candidate (item 19). |
| P35 | `core.excludesFile=/dev/null` exits 2 `unsupported-file` (`warrant-dxy`). | G5, carried item 3 | minor (fail-closed) | Fixed: lane A item A1. |
| P36 | `git::run_stream` deadlock hazard. | G6, O28, carried item 5 | minor (latent) | Fixed: lane A item A4 (both seats price it before any batch reader; done now). |
| P37 | `prefixed_git_oid` error branches untested (`lib.rs:889`). | G9, carried item 9 | minor (test) | Fixed: lane B item B20. |
| P38 | `GeneratedDeclaration.inputs` defaults to empty, so omitted and `[]` are indistinguishable (`manifest/mod.rs:129`). | G10, carried item 9 | minor | Fixed: lane B item B21 (`Option<Vec<String>>`). The spec 5.3 wording stays a clarification candidate. |
| P39 | Error documents can carry absolute home paths (`manifest.rs:24`, `cache.rs:71`). | G13 | note | Fixed: lane B item B22. |
| P40 | `warrant-cli` declares unused dependencies (P6 pre-declaration); `/usr/bin/find` hard-coded in `scripts/budget.sh`; `warrant inventory` is worktree-only; the cache has no read side. | G14, G15 | note | Recorded. `/usr/bin/find` is deliberate (the estate's shadowed `find` rule) and stays. Inventory kind selection and cache reads are named for the M1 plan amendment at W1.G. |

Refuted: none. Every finding verified at its cited line held; the coordinator disagrees with Astra only on severity for P8 (the product-side proof exists as a receipt; the gap is the missing reorder test and the table's wording) and on the M1-admission verdict, which is decided by the fix pass and its fresh review rather than by the head under review.

## Fresh review of the fix pass, and its dispositions

The fix diff `c0aaea6..13e45d0` (40 commits, 67 files, +4,257/−482) went to a fresh two-family panel in the same `safe` arrangement, with the diff, the three lane briefs and every followup, the P1–P40 dispositions, and the gate log at the candidate (redacted of corpus paths) under `review-input/`: Astra `xhigh` on Codex (codex-70) and GLM 5.3 on OMP (omp-33). Astra: "merge after the majors", seven majors and three minors; GLM: "merge", three minors and seven notes. Both seats rated all 39 lane items does-what-it-says with a named binding test, and GLM independently verified B14's six regenerated `files` counts as old minus ignored. The coordinator verified every cited line at `13e45d0`; no finding was refuted. Ruling F44 (on `warrant-29b`) sent the fixable findings through a second bounded fix round, with a single Astra re-check of the round-2 diff as the loop bound.

| # | Seat | Severity | Finding (where) | Verdict | Disposition |
|---|---|---|---|---|---|
| P41 | Astra 6, GLM 3 | major | Verify-generated compares canonical captured bytes with raw reproduced bytes (`crates/inventory/src/lib.rs` `verify_generated`, `blob_id`; A2×B19 seam): a faithful producer under a clean filter or eol conversion reports `generated-drift` | confirmed | fix, B26 (compare Git blob identities on both sides through `Snapshot::blob_id`, A14) |
| P42 | Astra 1 | major | The CLI keeps only `GeneratedDrift` from verification; a glob with one present and one missing output exits 0 with no absence recorded (`crates/cli/src/commands/inventory.rs`) | confirmed | fix, B25 |
| P43 | Astra 3 | major | Cargo discovery runs `cargo metadata` on the live `Cargo.toml` and decides on the live `Cargo.lock` (`discover_cargo_units`); B1's undisclosed remainder, pre-existing at `c0aaea6` | confirmed | fix, B24 |
| P44 | Astra 4 | major | An unborn nested repository created between capture and verification changes `CapturedTree.unborn` but not the tree id, so the id-only stability check publishes a snapshot without it (`crates/snapshot/src/lib.rs` `capture`) | confirmed | fix, A12 |
| P45 | Astra 5 | major | A tracked `package.json` symlink inside the snapshot is parsed as its target pathname since B1 (previously followed on disk); spec 5.6 | confirmed | fix, B28 through `Snapshot::resolve` (A14) |
| P46 | Astra 7 | major | Producers run with inherited stdout and stderr; a chatty producer writes before the JSON document | confirmed | fix, B27 |
| P47 | Astra 2 | major | Manifest loading and object capture resolve the revision separately (`load_snapshot_manifest`, `capture_once`); a ref moved between them yields a snapshot of commit B under commit A's limits | confirmed as a narrow race | fix, B34: resolve the commit once in the CLI and pass the object id to both (follow-on to B2/A5; assigned with round 2's second lane B followup) |
| P48 | GLM 2 | minor | A declared submodule whose checkout is unborn fails capture with a raw `git-failed` (`worktree.rs`, the `160000` branch) | confirmed | fix, A13; the Git oracle turned out to vary by version (2.47.3 refuses `git add -A`, 2.55.0 accepts it; CI run `35963405310`), so ruling F46 makes Warrant refuse `unborn-submodule` regardless and A15 makes the test observe its oracle |
| P49 | GLM 1 | minor | `summary.by_class` counts ignored and absent-generated entries that `summary.files` excludes; spec 5.5's example sums `by_class` to `files` with no `ignored` key | confirmed | fix, B33 (corpus expectations lose `by_class.ignored`, all six members) |
| P50 | GLM 8 | note | `exit_blocked` bypasses the cancellation override moved into `main` by B11 | confirmed | fix, B30 |
| P51 | GLM 9 | note | `git_ok` puts raw Git stderr into `manifest-io` reasons; B22's relative-path rule holds only on the tested branches | confirmed | fix, B31 |
| P52 | Astra 10 | minor | `InventoryDocument.snapshot` is optional and defaulted; schema admits a document without a snapshot | confirmed | fix, B32 (required; pre-release, no legacy documents) |
| P53 | Astra 8 | minor | `run.py` redacts `tempfile.gettempdir()` (`/tmp`) while `corpus.sh` scratches under `${TMPDIR:-/var/tmp}` | confirmed | fix, C6 |
| P54 | Astra 9, GLM 6 | minor | `deps.sh` recognises one gate-command spelling, does not require setup-node before the gate step, and ignores the msrv job's `RUSTUP_TOOLCHAIN` selector | confirmed | fix, C7 |
| P55 | GLM 5 | note | B8's untracked set is re-listed inside the consume closure, a second observation from the one the tree came from | confirmed | fix, B29 through `Snapshot::untracked` (A14) |
| P56 | GLM 4 | note | Bare repositories are refused `non-repository` since B3 (`git rev-parse --show-toplevel`) | confirmed | accepted with reason: v1 reads the worktree manifest and needs a checkout; the narrowing is recorded here and is a spec clarification candidate (4.1) |
| P57 | GLM 10a, 10b | note | C1's commit moved source archiving after dependency install (benign: `git archive <commit>`); A2 adds one `git cat-file` per worktree read | confirmed | recorded: the reorder stays (source digests unchanged across four CI runs); the per-read process cost is measured tolerable on Atlas in the gate and batches (`cat-file --batch`) when a budget says so |
| P58 | GLM (B23 row) | note | Object kinds never distinguish declared from undeclared gitlinks (no `.gitmodules` read) | confirmed, pre-existing M0 scope | accepted with reason: bead `warrant-vxc` for the first M1 task that reads submodules |
| P59 | Astra, GLM 7 | note | The supplied `fix-gate.log` ends at `gate: OK` without the acceptance lines | confirmed | recorded: the acceptance log at `13e45d0` is the coordinator's evidence (`acceptance: OK`, `final: 6 in-scope document(s) checked at M0`), cited above |

Round 2 (ruling F44): lane A delivered A12–A14 (`2e4c185`) and A15 (`0e56a4e`, the Git-oracle fix under F46); lane C delivered C6–C7 (`cfad761`); lane B delivered B24, B25, B27, B30–B33 (`a75fee5`) and then B26, B28, B29, B34 on the merged Snapshot API (`949130f`); the coordinator raised the snapshot budget under F45 (`d1f94f6`). Every item carries an observed red in its lane report (quoted on `warrant-29b`). The round-2 head `949130f` passed the coordinator's gate and `--final M0` with Atlas (evidence above). Recorded from the lane reports, no fix: `Snapshot::blob_id` writes a loose object as capture already does for every worktree file; the native-capture stub returns an empty untracked set (`warrant-ail`); a tsconfig reference to a symlinked directory is not followed; B24 is unverified on Cargo workspaces with path dependencies outside the repository (no corpus member is a Rust workspace). Astra re-check of the round-2 diff (`13e45d0..949130f`, one seat, `safe`, `xhigh`): "merge after the majors", three majors, two minors, one note. Its disposition table rated P42, P44, P45, P46, P48, P49, P51, P52 and P55 closed with strong binding tests, P43, P47, P50 and P54 narrowed, and P53 closed in code but unbound because the gate never ran its test. The coordinator verified all six at `949130f`; none refuted. Ruling F47 (inside F44's bound) ran one last coordinator-verified round with no further seat:

| # | Severity | Finding (where) | Disposition |
|---|---|---|---|
| P60 | major | A reproduced output that changes file type (regular to symlink or back) is hashed under the captured entry's mode, so a filtered regular file replaced by a symlink to its own contents reports clean (`reproduced_blob`, `Snapshot::blob_id`) | fix, B35: a file-type change is `generated-drift` before hashing |
| P61 | major | The Cargo temporary copy omits `rust-toolchain` and `rust-toolchain.toml`, so a toolchain-pinned repository runs `cargo metadata` under the default Cargo (regression from `c0aaea6`, where metadata ran in the repository root) | fix, B36: the captured toolchain files are copied and the command runs with `RUSTUP_AUTO_INSTALL=0`; an uninstalled pinned toolchain is a named `invalid-declaration` |
| P62 | major | A `--commit` revision that fails the first resolution falls through to capture as given; a ref created between the lookups yields a snapshot of the new commit under default limits (`resolve_commit`, `snapshot::run`) | fix, B37: a failed resolution is terminal `missing-commit` |
| P63 | minor | `deps.sh` reads the msrv job's `RUSTUP_TOOLCHAIN` only at job-env indentation; a step-level override is invisible | accepted with reason: the real workflow has none; bead `warrant-9xs` |
| P64 | minor | The C6 redaction test (`CorpusRunnerTests`) is never executed by the gate: the corpus stage enters `main()`, the test stage runs Cargo | fix, C8: a required stage runs the Python unit tests with their count |
| P65 | note | B30 fixes the exit code of a late signal; the cache artifact and the emitted document stand, so spec 10.2's no-artifact half is unmet on that path | accepted with reason: same family as `warrant-dpp` (comment added); W2.10's fault hook is the binding |

Round 3 result: lane B delivered B35 (`3dc7a9b`), B36 (`e0347a8`), B37 (`17622a9`), merged at `47deede`; lane C delivered C8 (`0d1c898`), merged at `f6f9583`, the W0.G fix candidate (23 non-merge commits since `13e45d0`, 63 since `c0aaea6`). Each carries an observed red in its lane report. Lane-stated limits, recorded: the B36 tests need a rustup-managed cargo, and the `RUSTUP_AUTO_INSTALL=0` guard was not red-proofed because removing it would start a real download; rustup directory overrides do not carry into the temporary copy, the toolchain files do. Coordinator verification at `f6f9583`: `scripts/gate.sh` printed `gate: OK` (32 test binaries, 183 passed, 0 failed; the corpus stage now prints `corpus: runner unit tests 1` before the six members; `budget: snapshot 2621/3000`), `tests/acceptance.sh --final M0` with the private Atlas member present exited 0 (`acceptance: OK`, `corpus: atlas ran`, `final: 6 in-scope document(s) checked at M0`), and CI run `35966837232` executed the gate green on `gate (stable)` and `gate (MSRV 1.96.0)`.

## Items carried to this gate, and their dispositions

Each item below was written to `warrant-29b` or live-state during M0 with "at W0.G" on it. Disposition vocabulary: fixed (with the lane item and commit), accepted with reason (open bead or plan row named), amended (plan, brief or script change made at this gate), recorded (no action; the record is the action).

1. **Native capture behind the feature, off** (`warrant-ail`, F33/F34, blocked W0.G). Accepted with reason: spec 21 M0 says S3 "resolves native capture or keeps the Git path"; the Git path is kept on measured grounds (six-case parity, corpus criterion not run, `Option<String>` return would drop carried and oversize accounting). The bead now blocks W1.G instead of W0.G, where the corpus criterion can be measured with Atlas provisioned. Overrulable by Trey.
2. **Cooperative signal honoured only after the walk** (`warrant-dpp`, W0.7-C3, F36, blocked W0.G). Accepted with reason: the no-partial-artifact half holds by construction (atomic cache write); the cooperative half is a latency defect, and W2.10's fault hook is where entry-granular cancellation gets its test. The bead now blocks W2.10. Lane B's B11 makes the CLI check the cancellation flag before reporting any snapshot error, with a process-group test, so a cancelled run no longer reports a capture error as its reason.
3. **`core.excludesFile=/dev/null` exits 2 `unsupported-file`** (`warrant-dxy`). Fixed: lane A item A1, `5645a72` (configured ignore files read with ordinary file I/O; a missing file contributes nothing; an unreadable one is `snapshot-io` naming the config key and path; the digest layout for regular files is unchanged). Bead closed with the merge.
4. **MSRV disagreement** (`warrant-b3s`). Fixed: lane C item C2, `0f2e0eb` (`[workspace.package] rust-version = "1.96"`, every crate `rust-version.workspace = true`, `scripts/deps.sh` checks each crate's effective value against the toolchain CI's `msrv` job runs, red-proofed by editing one manifest). Spec 18's 1.90 line (line 1926, from tree-sitter 0.27's floor) is a clarification candidate for Trey (item 19). Bead closed with the merge.
5. **`git::run_stream` deadlock hazard**. Fixed: lane A item A4, `1d360d8` (stdin is written from a scoped thread while the parent drains stdout and stderr). The lane could not reproduce the hang with `git stripspace` as the brief suggested, because stripspace consumes its whole input before writing; it reproduced it with a `!cat` alias that echoes as it reads, under a timeout guard, and the test keeps that shape.
6. **`warrant inventory --verify-generated` has no owner after W0.7** (spec 5.7). Fixed: lane B item B19 (flag wired to the existing `verify_generated`, `generated_drift` reported in the document, only non-ignored entries copied to the comparison tree; commit in the lane B round-2 list above).
7. **Classification-overlap rejection at construction, not manifest load** (W0.6 delivers). Accepted as equivalent for M0, recorded.
8. **NDJSON output absent** (W0.7 delivers, F36 carry). Ruling F43: paged JSON with digest-bound cursors is the v1 list convention; NDJSON is an M5 rendering mode and the W0.7 delivers text over-promised it. Bead `warrant-5o9` carries the bounded-list and cursor items for the M5 rendering task.
9. **`prefixed_git_oid` untested error branch; `GeneratedDeclaration.inputs` default ambiguity; spec 5.1 prefixed blob identity** (F34 ledger). Fixed: lane B items B20 (error-branch tests) and B21 (`inputs: Option<Vec<String>>`, omitted and empty distinguished in the document and the schema). The `inputs` semantics remain a spec 5.3 clarification candidate for Trey (item 19); the code now preserves the distinction so either answer is representable.
10. **Demo 3's module-owned assertions bound by crate test until W2.1** (F39) and **atomic-write invariant bound by construction until W2.10** (F37). Recorded; the proof table rows name the binding tasks.
11. **Expectation files encoding observed or environment-dependent behaviour** (two of W0.6's three executes; F39, F40). Amended: the thirty compiled M1 and M2 briefs that carry the test-skeptic persona now name the class ("an expectation file must state Git's or the spec's answer, not the product's observed output; an expectation that depends on the temporary path, ambient Git configuration or the platform is a finding"), applied in place under ruling F42 with backups `.pre-f42`. The frozen script is unchanged: the briefs are read at dispatch.
12. **Harness contract: rejection counts.** Twenty-three `agent_rejected` events in M0: execute 5, merge 6, fix 5, close 2, retry-readmission 3, unacknowledged-coordinator 2. Structured-output failures by rule: reviewer `FINDINGS` root "value must be 'array'" on the test-skeptic seat at W0.2, W0.3, W0.7 (retried) and exhausted at W0.1, W0.5, W0.8; adjudicator "value must be 'object'" at W0.5, W0.7, W0.8 (retried). Two or more on the same envelope rule, so the trigger written at W0.1 fires. Disposition: the in-run mitigation (reviewer timeouts 600 to 1800 for every unsettled task, applied 2026-09-20, 86 rows) stands; the envelope fix (`FINDINGS` root becomes an object; the delegate retry prompt re-carries the schema) is upstream in writing-plans and delegate, papercuts filed; the frozen script is not re-rendered because the schema sits inside every settled stage's digest. The M0 panels were effectively two-family on W0.1, W0.5 and W0.8; this record says so, and the M1 adjudicator counts a missing seat as a decorrelation loss (F12 part 1).
13. **Fix lanes and shared corpus artefacts** (W0.8, F16 carry). Amended with item 11: the same brief edit adds the rule that a fix lane records a digest only over an artefact it regenerated in that attempt.
14. **Identical-prompt retries against out-of-fence blockers** (W0.6: execute ×2 on the memo defect, fix ×2 on F1). Recorded; papercuts `pc2_e62ec824c2c4acb0` and `pc2_a08895b885fa4a60`.
15. **Plan owned_files amendments deferred to this gate.** W0.4's `crates/snapshot/Cargo.toml` was done at `c1ba0d7` (F36 window). W0.2's stage-file ownership (F11 follow-up): amended at this gate, `docs/plans/2026-09-19-warrant-v1-plan.md` W0.2 `owned_files` gains `scripts/stages/REQUIRED`, which W0.2 wrote and no row owned.
16. **Three minor-only fix tasks never dispatched at W0.2** (standard regime, fix floor major). Recorded with their ids in bead `warrant-qa6`: `fix-w02-cli-msrv` (lane1:W02-R3) is fixed by item 4; `fix-w02-core-contracts` (lane1:W02-R4, lane1:W02-R5, lane3:skeptic-w02-2, -3, -4: unknown-document error loses the name, class list not derived from `InventoryClass`, unpinned serialization defaults and version gate) and `fix-w02-fault-gate` (lane1:W02-R6: `pub mod fault;` ungated) stay open for the first M1 lane in `crates/core` or a coordinator lane before W1.G.
17. **Gate target directories**: eight orphan `gate-*` directories for vanished checkouts reaped at this gate (coordinator action approved 2026-09-20), about 22 GB.
18. **P15 escalation list revisited** (the plan row says "the list is revisited at W0.G"). Recorded, no change to P15. The list ran as written for nine waves: 43 rulings (F1 to F43) on `warrant-29b`, every park, every checkpoint, and every adjudication with disputed findings came to the coordinator seat, and none of them was a decision the driver could have taken alone. The "too wide" downside (the channel becomes a permission queue) did not appear, but for a reason P15 did not anticipate: from 2026-09-23 the coordinator seat and the orchestrator seat have been one session under Trey's word, so the escalation cost was a context switch rather than a wait. The "too narrow" downside did appear once, indirectly: the machinery's identical-prompt retry against an out-of-fence blocker (item 14) is not on the list because it is a harness behaviour, not a decision; it cost two lanes at W0.6. The M1 seats keep P15 as written and add nothing to the list; the retry defect is handled upstream (papercuts) and by the coordinator reading a rejection reason before the machinery retries.
19. **Spec clarification candidates for Trey** (not rulings, listed for his next spec pass): `warrant.commands` as a twenty-fourth schema against spec 1.6 and 12.1 (F36); `GeneratedDeclaration.inputs` omitted versus empty (5.3); the MSRV line (18, line 1926) against the 1.96 the workspace now declares; D2 identifiers before durable signing; the case-collision outcome (4.3 says `analysis: incomplete`; the product exits 2 until the verdict facet exists, bead `warrant-6v7`); the error-code vocabulary (12.1 does not enumerate codes: `nested-repository` from 5.6, `unborn-submodule` from A13, `cache-location` from F42, and the `warrant.error/1` codes the CLI tests pin should be listed once).

## Reviewer-seat notes for M1

- Lane A's A10 isolates the Git environment (`GIT_CONFIG_GLOBAL`, `GIT_CONFIG_NOSYSTEM`, `HOME`, `XDG_CONFIG_HOME`) only in test builds, through `#[cfg(test)] crate::tests::neutralize_git_environment(&mut command)` in `crates/snapshot/src/git.rs`; production commands inherit the user's configuration as before. The fresh review was asked to confirm no `cfg(test)` leak; see its findings.
- The snapshot crate's line budget is 2396 of 2500 after the fix pass; the next task that grows it (W1.x native capture or the read path) should split `tests.rs` before adding to it.

## What this gate did not do

- close:5 has not run: the W0.G checkpoint fired on W0.6's verify-fixes result and the pause unwound the close item before it started. It runs on resume, after this gate's approval; a refusal there parks and is handled by the coordinator without reopening this record.
- The panels did not see the private Atlas member's contents, and no lane did; the member ran only under the coordinator's `WARRANT_CORPUS_DIR` and in lane B's own gate on this machine. Durable provisioning of the private runner stays Trey's (`warrant-udz`).
- No third review round: ruling F44 bounded the loop at one re-check of the round-2 diff; its majors, if any, were fixed and gate-verified by the coordinator without another seat (recorded above).
