# Unowned source: positive

Hard control: every enabled-language source belongs to at most one declared
module. This adapted Specgate basic-project variant owns its only source.
Siblings `unowned-source-negative` and `unowned-source-break-control` cover an
unclaimed non-source and an unclaimed first-party source.
