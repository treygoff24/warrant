[package]
name = "warrant-lang-ts"
version.workspace = true
edition.workspace = true
rust-version = "1.96"
license.workspace = true

[dependencies]
warrant-core = { version = "0.1.0", path = "../core" }
warrant-model = { version = "0.1.0", path = "../model" }
